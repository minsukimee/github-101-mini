name: CI Demo

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run demo step
        run: echo "CI ran on $(date)"

      - name: Check that README exists
        run: test -f README.md


